-- Vincular dueño existente (Cruje, Betta, etc.) tras crear su usuario en Supabase Auth.
-- Reemplaza el UUID y el email.

-- UPDATE businesses
-- SET owner_user_id = 'UUID-DEL-USUARIO-AUTH'
-- WHERE slug = 'cruje';

-- Verificar:
-- SELECT id, name, slug, owner_user_id FROM businesses WHERE slug = 'cruje';
